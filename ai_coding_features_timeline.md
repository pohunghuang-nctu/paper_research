# AI 輔助開發功能發展時間軸

這個時間軸展示了從單純的程式碼完成到具備自主性的 Agentic 模式的演進過程。

---

### **第一階段：生成式程式碼完成 (Generative Code Completion)**

*   **時間點:** **2021 年 6 月**
*   **代表性功能/產品:** **GitHub Copilot (技術預覽版發布)**
*   **核心概念:** 在開發者撰寫程式碼時，AI 能夠根據當前的程式碼上下文 (context)，自動建議並生成單行或整個程式碼區塊 (block)。

---

### **階段 1.5: 次世代編輯預測 (Next Edit Prediction / Diff Prediction)**

*   **時間點:** **約 2023 年 - 2024 年**
*   **代表性功能/產品:** **Zed Editor (搭載 Zeta 模型), CarperAI (基於 CodeGen 的 Diff 模型)**
*   **核心概念:** 這項技術超越了傳統的程式碼補全。它不再只是預測下一個詞 (token) 或填補中間的空白 (Fill-in-the-middle)，而是預測開發者的 **下一個完整編輯動作**。模型會直接生成一個「差異 (diff)」，能夠同時包含程式碼的新增、刪除與修改，實現更複雜的重構或程式碼調整。
*   **對應 AI 任務類別:** **程式碼重寫 (Code Rewriting) / 差異預測 (Diff Prediction)**
    *   **註記:** 這個階段的關鍵轉變是將 AI 任務從「生成」變為「編輯」。以 Zed 的 Zeta 模型為例，它透過重寫游標周圍的程式碼片段來實現預測，使其能處理更複雜的多行編輯任務。另一個趨勢是模型的開放性，例如 Zed 將其 Zeta 模型及訓練資料集開源，而 CarperAI 的 Diff 模型也是基於開源的 CodeGen 建立，這促進了社群的共同發展。

---

### **第二階段：通用對話式 AI (Conversational AI)**

*   **時間點:** **2022 年 11 月**
*   **代表性功能/產品:** **ChatGPT (公開發布)**
*   **核心概念:** 開發者可以透過自然語言與 AI 進行對話，詢問程式設計問題、請求程式碼範例、除錯建議或解釋複雜的演算法。雖然不是直接整合在 IDE 中，但它開創了使用對話來解決程式設計問題的全新模式。
*   **對應 AI 任務類別:** **指令遵循 (Instruction Following) / 對話式 AI (Conversational AI)**
    *   **註記:** 以 ChatGPT 為代表的通用對話模型，展現了強大的零樣本 (zero-shot) 和少樣本 (few-shot) 學習能力。它能理解複雜的自然語言指令，並生成符合要求的、結構化的回答，為後續的 IDE 內嵌聊天功能奠定了基礎。

---

### **第三階段：IDE 內嵌式聊天 (Inline Chat)**

*   **時間點:** **約 2023 年 3 月** (GitHub Copilot Chat 進入公開技術預覽)
*   **代表性功能/產品:** **GitHub Copilot Chat**
*   **核心概念:** 將對話式 AI 直接嵌入到程式碼編輯器中。開發者無需切換視窗，可以直接在 IDE 內選取一段程式碼，然後透過聊天視窗提問，例如「解釋這段程式碼」、「為此函式產生單元測試」或「重構這段程式碼，使其更有效率」。
*   **對應 AI 任務類別:** **上下文感知對話 (Context-Aware Conversation) / 指令遵循 (Instruction Following)**
    *   **註記:** 這是通用對話 AI 在軟體開發領域的垂直應用。它結合了 LLM 的對話能力和對「當前編輯器上下文」的理解能力，使 AI 的輔助更具針對性和實用性。

---

### **第四階段：自主代理寫作模式 (Agentic Write Mode)**

*   **時間點:** **約 2023 年中後期 ~ 2024 年**
*   **代表性功能/產品:** **Cursor, Windsurf Cascade**
*   **核心概念:** AI 不再只是被動地根據指令生成程式碼，而是能化身為一個「軟體開發代理 (Agent)」。它可以理解一個更宏觀的開發任務（例如「新增一個使用者登入的 API 端點」），並自主地進行規劃、搜尋整個專案的程式碼、決定需要修改哪些檔案、並自動執行這些修改。使用者從「指令下達者」轉變為「監督者」。
*   **對應 AI 任務類別:** **自主代理 (Autonomous Agents) / 規劃與工具使用 (Planning and Tool Use)**
    *   **註記:** Agentic AI 是目前發展的前沿。其核心是讓 LLM 作為一個「大腦」或「推理引擎 (Reasoning Engine)」，不僅能生成內容，還能使用外部工具（如檔案系統讀寫、程式碼搜尋、執行終端機指令等）來完成複雜任務。這種模式需要模型具備任務拆解、自我反思和多步驟推理的能力。

---

### **總結**

從這條時間軸可以看出，GAI 在輔助開發領域的演進，是一個**從「被動建議」到「主動執行」、從「單點生成」到「全域協作」**的過程。AI 在開發流程中的角色，也從一個單純的「自動補全工具」逐漸演變為「對話夥伴」，並最終朝向一個能夠自主完成開發任務的「虛擬團隊成員」發展。
