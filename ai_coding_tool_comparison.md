# AI 程式碼輔助工具「編輯預測」功能比較

這份文件比較了各大 AI 程式碼輔助工具中，與 Zed Editor 的「編輯預測」(Edit Prediction) 功能類似的特性。

## Zed 「編輯預測」的核心特點

- **主動預測性**：在使用者輸入的間隙，主動預測下一步可能要做的整個編輯，而不僅僅是補全當前行。
- **重寫機制**：通過「重寫」(Rewrite) 程式碼區塊來實現預測，使其能執行更複雜的操作。
- **無縫互動**：主要通過 `Tab` 鍵一鍵接受，體驗流暢。

---

## 各大工具功能比較

| 工具 | 功能名稱 (類似) | 運作方式 & 互動模型 | 與 Zed 的主要差異 |
| :--- | :--- | :--- | :--- |
| **Zed** | **Edit Prediction** | **預測與重寫**：在你輸入的間隙，主動預測下一步的程式碼修改。模型會重寫游標附近的一塊程式碼，並將預測的修改包含在內。使用者按 `Tab` 接受。 | (基準) |
| **GitHub Copilot** | **Inline Suggestions / Next Edit Suggestions** | **預測與補全**：核心是程式碼補全，近年也引入了「下一個編輯建議」，能預測多行修改。互動也是通過 `Tab` 接受。其重構功能更多依賴 Copilot Chat 的明確指令。 | **哲學差異**：Copilot 的核心仍是「補全」，而 Zed 從一開始就圍繞「重寫」設計。雖然最終效果可能相似，但 Zed 的方法在理論上可能更容易處理複雜的、非連續的程式碼重構。 |
| **Cursor** | **Predictive Editing** | **預測與重寫**：作為「AI-First」編輯器，其核心功能與 Zed 非常接近。它同樣會預測下一步編輯，並允許一鍵接受，能夠預測多行編輯並根據最近變更調整。 | **相似度極高**：Cursor 和 Zed 在此功能上理念和實現非常相似，是直接競爭者。差異主要在於背後模型、訓練資料、性能優化和整合細節。 |
| **Windsurf** | **Smart Fix / Supercomplete** | **修正與增強補全**：當偵測到程式碼中的潛在問題時，會主動提供修正建議 (Smart Fix)。其 Supercomplete 功能不僅是補全，更能預測並生成完整的程式碼區塊，超越傳統的自動完成。 | **側重不同**：Windsurf 的方法更側重於**修正 (Fixing)** 和**增強型補全 (Enhanced Completion)**。它可能在偵測到錯誤或有明確改進模式時觸發，而 Zed 的「編輯預測」則更廣泛地預測開發者的下一步意圖，即使當前程式碼沒有明顯錯誤。 |

---

## 總結與見解

1.  **兩種主流範式**：
    *   **無縫預測流 (Zed, Cursor)**：將 AI 深度整合到開發者的每一次按鍵中，創造流暢、無干擾的「人機協同」體驗，如同開發者的「潛意識」。
    *   **指令控制流 (Copilot Chat)**：更像是與一個「AI 結對程式設計師」對話。使用者負責下達指令和決策，AI 負責分析和執行。在處理複雜、目標明確的任務時可能更有效率。像我這樣的 Agentic AI 也屬於此類。

2.  **GitHub Copilot 的演進**：Copilot 正逐漸向這兩種範式擴展。它的「Next Edit Suggestions」是在向「預測流」靠攏，而強大的「Copilot Chat」則是在「指令控制流」上深耕。

3.  **沒有絕對的好壞，只有場景的區別**：
    *   當專注編寫新功能時，**Zed** 或 **Cursor** 的預測功能可能會極大地提升「心流」體驗。
    *   當需要進行大規模重構或修復棘手 Bug 時，與 **Copilot Chat** 或其他 Agentic AI 進行對話，下達明確指令，可能是更可靠的選擇。
